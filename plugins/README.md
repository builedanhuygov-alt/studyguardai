# Example plugins

Standalone example plugins showing the marketplace pattern. They are **not**
installed with the core package; import them (or ship them as their own package
with an entry point) to register.

- `pomodoro_plugin.py` — a complete `DataSource` emitting planned study minutes.

See `docs/PLUGINS.md` for how to author and publish one.
