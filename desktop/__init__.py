"""Native desktop shell for StudyGuard AI.

Orchestration only (window, tray, settings, notifications, logging). All business
logic stays in the domain and is reached exclusively through StudyGuardService /
the REST API — identical to the web dashboard.
"""
